public class MySorts {
    public static void mergesort(IList) {
        
    }

    private static IList mergesortHelper(IList){
        int mid =a.length/2;
        int[] left =Arrays.copyOfRange(a,0,mid);
    }

    private static IList getLeftHalf(IList) {
        int mid =a.length/2
        int[] right =Arrays.copyOfRange(a,mid,a.length);
    }

    private static IList getRightHalf(IList) {

    }
}
